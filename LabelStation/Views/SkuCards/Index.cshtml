@model IEnumerable<LabelStation.Models.SkuCard>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout_Hudson.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ItemNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ItemNumber)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SkuCardId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SkuCardId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SkuCardId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
